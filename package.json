{
  "name": "chatgpt clone",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\" \"python ai-rag-service/app.py\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
